# Core
watchdog>=4.0.0
python-dotenv>=1.0.0
schedule>=1.2.0

# Gmail Watcher (optional — needed only if using Gmail integration)
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.122.0

# WhatsApp Watcher (Silver tier — optional)
playwright>=1.43.0
